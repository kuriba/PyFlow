# TODO write FlowRunner class which handles moving molecules between workflow
#  steps. Should be created each time a workflow step ends. Should handle
#  processing of data between the steps. Use an entry point to allow quick
#  command line access to FlowRunner
